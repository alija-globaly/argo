################  reference : https://gateway-api.sigs.k8s.io/guides/getting-started/#installing-gateway-api
############### reference : https://gateway-api.sigs.k8s.io/guides/getting-started/
---
# First: Install Gateway API CRDs
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-api-crds
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"  # Installing crd before aws elb install
spec:
  project: default  # or 'infra' if you prefer
  source:
    repoURL: https://github.com/kubernetes-sigs/gateway-api.git
    targetRevision: v1.4.1
    path: config/crd/experimental
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - Replace=true
      - PruneLast=true
      - PrunePropagationPolicy=foreground
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aws-load-balancer-controller
  namespace: argocd
spec:
  project: infra

  sources:
    - repoURL: https://aws.github.io/eks-charts
      chart: aws-load-balancer-controller
      targetRevision: 3.0.0
      helm:
        valueFiles:
          - $values/gitops/values/staging/root-app-values/aws-elb-values.yml

    - repoURL: https://github.com/GlobalyHub/agentcis-app-gitops.git
      targetRevision: "staging"
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PruneLast=true 
      - PrunePropagationPolicy=foreground      