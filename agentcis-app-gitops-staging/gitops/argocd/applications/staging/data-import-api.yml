apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentcis-data-import-backend-test
  namespace: argocd
  # finalizers:
  #   - resources-finalizer.argocd.argoproj.io/background  

spec:
  project: default
  source:
    repoURL: "https://github.com/alija-globaly/argo.git"
    targetRevision: "main"          # branch to deploy
    path: "agentcis-app-gitops-staging/gitops/charts/self-managed/backend" # path to the Helm chart
    helm:
      valueFiles:
        - ../../../gitops/values/staging/root-app-values/data-import-api-values.yml   # your backend values file
  destination:
    server: https://kubernetes.default.svc
    namespace: agentcis-data-import-backend-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PruneLast=true       
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground 
