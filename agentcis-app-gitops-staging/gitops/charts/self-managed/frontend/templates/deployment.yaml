apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontend.name }}-deployment
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.frontend.name }}

spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.frontend.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.frontend.name }}
    spec:
      {{- if .Values.frontend.imagePullSecrets.enabled }}
      imagePullSecrets:
      - name: {{ .Values.frontend.imagePullSecrets.name }}
      {{- end }}
      containers:
      - name: {{ .Values.frontend.name }}
        image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
        imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
        {{- if .Values.frontend.startupCommand.enabled }}
        command: 
          - {{ .Values.frontend.startupCommand.shell | quote }}
          - "-c"
        args:
          - |
{{ .Values.frontend.startupCommand.command | indent 12 }}
        {{- end }}
        ports:
        - containerPort: {{ .Values.frontend.service.containerport }}
   
        {{- if .Values.frontend.livenessProbe.enabled }}
        livenessProbe:
          httpGet:
            path: {{ .Values.frontend.livenessProbe.httpGet.path }}
            port: {{ .Values.frontend.httpPort }}
          initialDelaySeconds: {{ .Values.frontend.livenessProbe.initialDelaySeconds | default 15 }}
          periodSeconds: {{ .Values.frontend.livenessProbe.periodSeconds | default 30 }}
          failureThreshold: {{ .Values.frontend.livenessProbe.failureThreshold | default 3 }}
        {{- end }}

        {{- if .Values.frontend.readinessProbe.enabled }}
        readinessProbe:
          httpGet:
            path: {{ .Values.frontend.readinessProbe.httpGet.path }}
            port: {{ .Values.frontend.httpPort }}
          initialDelaySeconds: {{ .Values.frontend.readinessProbe.initialDelaySeconds | default 15 }}
          periodSeconds: {{ .Values.frontend.readinessProbe.periodSeconds | default 30 }}
          failureThreshold: {{ .Values.frontend.readinessProbe.failureThreshold | default 3 }}
        {{- end }}

        {{- if .Values.frontend.startupProbe.enabled }}
        startupProbe:
          httpGet:
            path: {{ .Values.frontend.startupProbe.httpGet.path }}
            port: {{ .Values.frontend.httpPort }}
          initialDelaySeconds: {{ .Values.frontend.startupProbe.initialDelaySeconds | default 15 }}
          periodSeconds: {{ .Values.frontend.startupProbe.periodSeconds | default 30 }}
          failureThreshold: {{ .Values.frontend.startupProbe.failureThreshold | default 3 }}
        {{- end }}

        {{- if .Values.frontend.volumes.enabled }}
        volumeMounts:
        {{- if .Values.frontend.volumes.env.enabled }}
        - name: env-volume
          mountPath: {{ .Values.frontend.volumes.env.nginxOneMountPath | quote }}
          subPath: {{ .Values.frontend.volumes.env.nginxOneSubPath }}
        - name: env-volume
          mountPath: {{ .Values.frontend.volumes.env.nginxTwoMountPath | quote }}
          subPath: {{ .Values.frontend.volumes.env.nginxTwoSubPath }}
        {{- end }}

        {{- if .Values.frontend.volumes.logs.enabled }}
        - name: log-volume
          mountPath: {{ .Values.frontend.volumes.logs.mountPath | quote }}
          subPath: {{ .Values.frontend.volumes.logs.subPath }}
        {{- end }}
        {{- end }}
        resources:
          {{- toYaml .Values.frontend.resources | nindent 10 }}
      {{- if .Values.frontend.affinity.enabled }}
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          {{- range .Values.frontend.affinity.preferredNodes }}
          - weight: {{ .weight }}
            preference:
              matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - {{ .hostname }}
          {{- end }}
      {{- end }}
      {{- if .Values.frontend.volumes.enabled }}
      volumes:
      {{- if .Values.frontend.volumes.env.enabled }}
      - name: env-volume
        persistentVolumeClaim:
          claimName: {{ .Values.frontend.volumes.env.pvcName }}
      {{- end }}
      {{- if .Values.frontend.volumes.logs.enabled }}
      - name: log-volume
        persistentVolumeClaim:
          claimName: {{ .Values.frontend.volumes.logs.pvcName }}
      {{- end }}
      {{- end }}