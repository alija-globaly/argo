---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.auth.pv.config.name }}
spec:
  capacity:
    storage: {{ .Values.auth.pv.config.capacity | default "5Gi" }}
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: {{ .Values.auth.pv.config.reclaimPolicy | default "Retain" }}
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    path: {{ .Values.auth.pv.config.nfsPath }}
    server: {{ .Values.auth.pv.config.nfsServer }}



---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.auth.volumes.env.pvcName }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.auth.pv.config.capacity | default "5Gi" }}
  volumeName: {{ .Values.auth.pv.config.name }}
  storageClassName: ""