{{- if .Values.auth.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.auth.name }}-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.auth.ingress.clusterIssuer }}
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
  labels:
    app: {{ .Values.auth.name }}-ingress
spec:
  ingressClassName: {{ .Values.auth.ingress.className }}
  {{- if and .Values.auth.ingress.enabled .Values.auth.ingress.tlsEnabled }}
  tls:
  - hosts:
    - {{ .Values.auth.ingress.host }}
    secretName: {{ .Values.auth.name }}-tls
  {{- end }}
  rules:
  - host: {{ .Values.auth.ingress.host }}
    http:
      paths:
      - path: {{ .Values.auth.ingress.path }}
        pathType: Prefix
        auth:
          service:
            name: {{ .Values.auth.name }}-svc
            port:
              number: {{ .Values.auth.service.port }}
{{- end }}