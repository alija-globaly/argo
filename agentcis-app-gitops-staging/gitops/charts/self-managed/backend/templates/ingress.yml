{{- if .Values.backend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.backend.name }}-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.backend.ingress.clusterIssuer }}
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
  labels:
    app: {{ .Values.backend.name }}-ingress
spec:
  ingressClassName: {{ .Values.backend.ingress.className }}
  {{- if and .Values.backend.ingress.enabled .Values.backend.ingress.tlsEnabled }}
  tls:
  - hosts:
    - {{ .Values.backend.ingress.host }}
    secretName: {{ .Values.backend.name }}-tls
  {{- end }}
  rules:
  - host: {{ .Values.backend.ingress.host }}
    http:
      paths:
      - path: {{ .Values.backend.ingress.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.backend.name }}-svc
            port:
              number: {{ .Values.backend.service.port }}
{{- end }}